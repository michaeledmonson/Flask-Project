# Neon Postgres connection string.
DATABASE_URL=

# Anthropic API key — used only by the daily ingestion pipeline.
ANTHROPIC_API_KEY=

# Shared secret for GET /api/cron/ingest. Vercel Cron sends it automatically as
# `Authorization: Bearer $CRON_SECRET` once this is set on the project.
CRON_SECRET=
